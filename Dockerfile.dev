# ── Stage 4: runtime ──────────────────────────────────────────────────────────
FROM alpine AS runtime

WORKDIR /app

COPY ./target/x86_64-unknown-linux-musl/release/anvil-notify ./anvil-notify
COPY ./migrations  ./migrations
COPY ./config      ./config

EXPOSE 8080

ENTRYPOINT ["./anvil-notify"]